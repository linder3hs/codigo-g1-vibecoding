import { AppRouter } from "./routes";

/**
 * Main App component with React Router configuration
 * Entry point for the Todo application with routing
 */
function App() {
  return <AppRouter />;
}

export default App;
